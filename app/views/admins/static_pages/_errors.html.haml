- if @static_page.errors.any?
  #error_explanation
    %h2
      = pluralize(@static_page.errors.count, "error")
      prevented this record from being saved:
    %ul
      - @static_page.errors.full_messages.each do |msg|
        %li= msg